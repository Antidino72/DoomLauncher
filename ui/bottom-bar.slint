import { Button } from "std-widgets.slint";

export component BottomBar inherits Rectangle {

    callback launch();

    Button {
        text: "Start";

        x: 24px;
        y: 0px;

        clicked => {
            root.launch();
        }
    }
}